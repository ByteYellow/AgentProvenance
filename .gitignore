.DS_Store

# Local AgentProvenance state and build outputs.
/agentprov
/agentprov-sensor
/.agentprov/
/.agentprov-*/
/.acf/
/.acf-*/
# Root-level daemon logs: the dir globs above only match directories, so these
# stray .daemon.log files slipped into git once. Match them explicitly.
*.daemon.log

# Python build / packaging outputs (setuptools). Never commit these.
build/
*.egg-info/
__pycache__/

# Internal planning notes. Keep these local only.
01-atomized-design-online-CN.MD
02-paper-algorithm-map-CN.MD
03-readme-showcase-CN.MD
04-enterprise-session-profile-CN.MD
docs/north-star-three-pillars.md
README.zh-CN.local.md
HANDOFF.md
TODO.md

# Generated eBPF sensor artifacts. The bpf2go bindings (sensorbpf_bpf*.go/.o)
# ARE committed so `go build ./cmd/agentprov-sensor` works on a fresh Linux
# checkout without clang (regenerate via `go generate ./internal/sensor` after
# editing exec.c; CI guards drift). Only the huge generated vmlinux.h stays out.
internal/sensor/vmlinux.h

# 本地面试准备文档(不上传)
面试解读.zh-CN.md
docs/design-tls-intent-and-generality.md
demo/llm-judge/.judge-work/
demo/llm-judge/.build/

# Forensics captures can be tens of MB as raw JSON. Commit compressed replay
# fixtures (*.forensics.json.gz) or publish large raw captures as release assets.
*.forensics.json

# Portable release archives.
/dist/
